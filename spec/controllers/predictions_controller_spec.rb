require 'rails_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to specify the controller code that
# was generated by Rails when you ran the scaffold generator.
#
# It assumes that the implementation code is generated by the rails scaffold
# generator.  If you are using any extension libraries to generate different
# controller code, this generated spec may or may not pass.
#
# It only uses APIs available in rails and/or rspec-rails.  There are a number
# of tools you can use to make these specs even more expressive, but we're
# sticking to rails and rspec-rails APIs to keep things simple and stable.
#
# Compared to earlier versions of this generator, there is very limited use of
# stubs and message expectations in this spec.  Stubs are only used when there
# is no simpler way to get a handle on the object needed for the example.
# Message expectations are only used when there is no simpler way to specify
# that an instance is receiving a specific message.

RSpec.describe PredictionsController, type: :controller do

  # This should return the minimal set of attributes required to create a valid
  # Prediction. As you add validations to Prediction, be sure to
  # adjust the attributes here as well.
  let(:valid_attributes) {
    skip("Add a hash of attributes valid for your model")
  }

  let(:invalid_attributes) {
    skip("Add a hash of attributes invalid for your model")
  }

  # This should return the minimal set of values that should be in the session
  # in order to pass any filters (e.g. authentication) defined in
  # PredictionsController. Be sure to keep this updated too.
  let(:valid_session) { {} }

  describe "GET #index" do
    it "assigns all predictions as @predictions" do
      prediction = Prediction.create! valid_attributes
      get :index, {}, valid_session
      expect(assigns(:predictions)).to eq([prediction])
    end
  end

  describe "GET #show" do
    it "assigns the requested prediction as @prediction" do
      prediction = Prediction.create! valid_attributes
      get :show, {:id => prediction.to_param}, valid_session
      expect(assigns(:prediction)).to eq(prediction)
    end
  end

  describe "GET #new" do
    it "assigns a new prediction as @prediction" do
      get :new, {}, valid_session
      expect(assigns(:prediction)).to be_a_new(Prediction)
    end
  end

  describe "GET #edit" do
    it "assigns the requested prediction as @prediction" do
      prediction = Prediction.create! valid_attributes
      get :edit, {:id => prediction.to_param}, valid_session
      expect(assigns(:prediction)).to eq(prediction)
    end
  end

  describe "POST #create" do
    context "with valid params" do
      it "creates a new Prediction" do
        expect {
          post :create, {:prediction => valid_attributes}, valid_session
        }.to change(Prediction, :count).by(1)
      end

      it "assigns a newly created prediction as @prediction" do
        post :create, {:prediction => valid_attributes}, valid_session
        expect(assigns(:prediction)).to be_a(Prediction)
        expect(assigns(:prediction)).to be_persisted
      end

      it "redirects to the created prediction" do
        post :create, {:prediction => valid_attributes}, valid_session
        expect(response).to redirect_to(Prediction.last)
      end
    end

    context "with invalid params" do
      it "assigns a newly created but unsaved prediction as @prediction" do
        post :create, {:prediction => invalid_attributes}, valid_session
        expect(assigns(:prediction)).to be_a_new(Prediction)
      end

      it "re-renders the 'new' template" do
        post :create, {:prediction => invalid_attributes}, valid_session
        expect(response).to render_template("new")
      end
    end
  end

  describe "PUT #update" do
    context "with valid params" do
      let(:new_attributes) {
        skip("Add a hash of attributes valid for your model")
      }

      it "updates the requested prediction" do
        prediction = Prediction.create! valid_attributes
        put :update, {:id => prediction.to_param, :prediction => new_attributes}, valid_session
        prediction.reload
        skip("Add assertions for updated state")
      end

      it "assigns the requested prediction as @prediction" do
        prediction = Prediction.create! valid_attributes
        put :update, {:id => prediction.to_param, :prediction => valid_attributes}, valid_session
        expect(assigns(:prediction)).to eq(prediction)
      end

      it "redirects to the prediction" do
        prediction = Prediction.create! valid_attributes
        put :update, {:id => prediction.to_param, :prediction => valid_attributes}, valid_session
        expect(response).to redirect_to(prediction)
      end
    end

    context "with invalid params" do
      it "assigns the prediction as @prediction" do
        prediction = Prediction.create! valid_attributes
        put :update, {:id => prediction.to_param, :prediction => invalid_attributes}, valid_session
        expect(assigns(:prediction)).to eq(prediction)
      end

      it "re-renders the 'edit' template" do
        prediction = Prediction.create! valid_attributes
        put :update, {:id => prediction.to_param, :prediction => invalid_attributes}, valid_session
        expect(response).to render_template("edit")
      end
    end
  end

  describe "DELETE #destroy" do
    it "destroys the requested prediction" do
      prediction = Prediction.create! valid_attributes
      expect {
        delete :destroy, {:id => prediction.to_param}, valid_session
      }.to change(Prediction, :count).by(-1)
    end

    it "redirects to the predictions list" do
      prediction = Prediction.create! valid_attributes
      delete :destroy, {:id => prediction.to_param}, valid_session
      expect(response).to redirect_to(predictions_url)
    end
  end

end
