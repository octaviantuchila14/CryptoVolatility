<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @currency.name %>
</p>

<%=form_for @currency.neural_network do |f| %>
    <%= f.label "Days" %><br />
    <%= f.text_field :prediction_days %>
    <%= f.submit "Predict", class: "btn btn-primary" %>
<% end %>

<%= link_to 'Edit', edit_currency_path(@currency) %> |
<%= link_to 'Back', currencies_path %>
