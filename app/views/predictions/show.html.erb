<p id="notice"><%= notice %></p>


<%= javascript_include_tag "//www.google.com/jsapi", "chartkick" %>


<%= "Historic data" %>
<%= line_chart estimates_as_hash(@prediction.neural_network.exchange_rates) %>
<%= "Predicted data so far" %>
<%= line_chart estimates_as_hash(@prediction.past_estimates) %>
<%= "Future estimations" %>
<%= line_chart estimates_as_hash(@prediction.future_estimates) %>

<p>
  <strong>Average difference:</strong>
</p>
<p>
  <strong>Chi squared</strong>
</p>

<%= link_to 'Edit', edit_prediction_path(@prediction) %> |
<%= link_to 'Back', predictions_path %>
