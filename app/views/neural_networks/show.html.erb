<%= javascript_include_tag "//www.google.com/jsapi", "chartkick" %>

<text>
  Future predictions obtained with Machine Learning
</text><br><br>

<%= "Currency Historic data" %>
<% pp "exchange rates for predicted currency #{@pred_currency.full_name}, are #{@pred_currency.exchange_rates.size}"%>
<%= line_chart exchange_rates_as_hash(@pred_currency.exchange_rates), library: {vAxis: {title: "US Dollars"}} %>

<text>
  Estimation for the following week.
</text><br><br>

<%= "Neural network with backpropagation estimation" %>
<%= line_chart [{name: "Predictions", data: exchange_rates_as_hash(@nn_ers)},
                {name: "Previous Week", data: exchange_rates_as_hash(@pred_currency.last_week_rates)}],
                library: {vAxis: {title: "US Dollars"}, colors: ["red", "blue"]} %>

<text>
  You want to find out more about the currency?
</text><br>
<%= link_to 'Back to the currency\'s page', currency_path(@pred_currency)%>
<br><br><br>

<text>
  You want to find out how news articles will affect future prices?
</text>
<br>
<%= link_to 'Back to main page', currencies_path %>


<%# line_chart [
                       {name: "Series A", data: series_a},
                       {name: "Series B", data: series_b}
               ] %>
